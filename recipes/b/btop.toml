# Generated from Homebrew formula (BSD-2-Clause)

[package]
name = "btop"
version = "1.4.7"
revision = 2
description = "Resource monitor. C++ version and continuation of bashtop and bpytop"
license = "Apache-2.0"
homepage = "https://github.com/aristocratos/btop"

[source]
repo = "aristocratos/btop"
url = "https://github.com/aristocratos/btop/archive/refs/tags/v1.4.7.tar.gz"
sha256 = "933de2e4d1b2211a638be463eb6e8616891bfba73aef5d38060bd8319baeefc6"

[build]
steps = [
  "cmake -B builddir -DCMAKE_INSTALL_PREFIX=${PREFIX} -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=OFF",
  "cmake --build builddir -j ${JOBS}",
  "cmake --install builddir",
]

[build.linux-amd64]
toolchain = "llvm"
steps = [
  "cmake -B builddir -DCMAKE_INSTALL_PREFIX=${PREFIX} -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=OFF",
  "cmake --build builddir -j ${JOBS}",
  "cmake --install builddir",
]

[build.linux-arm64]
toolchain = "llvm"
steps = [
  "cmake -B builddir -DCMAKE_INSTALL_PREFIX=${PREFIX} -DCMAKE_BUILD_TYPE=Release -DBUILD_TESTING=OFF",
  "cmake --build builddir -j ${JOBS}",
  "cmake --install builddir",
]

[dependencies]
build = ["cmake"]

[dependencies.linux-amd64]
build = ["cmake", "llvm"]

[dependencies.linux-arm64]
build = ["cmake", "llvm"]
