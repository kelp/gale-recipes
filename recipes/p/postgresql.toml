[package]
name = "postgresql"
version = "18.3"
description = "PostgreSQL client tools (psql, pg_dump, pg_restore)"
license = "PostgreSQL"
homepage = "https://www.postgresql.org/"

[source]
repo = "postgres/postgres"
url = "https://ftp.postgresql.org/pub/source/v18.3/postgresql-18.3.tar.bz2"
sha256 = "d95663fbbf3a80f81a9d98d895266bdcb74ba274bcc04ef6d76630a72dee016f"

[build]
steps = [
  "./configure --prefix=${PREFIX} --with-openssl --without-icu",
  "make -C src/interfaces -j${JOBS}",
  "make -C src/bin -j${JOBS}",
  "make -C src/interfaces install",
  "make -C src/bin install",
  "make -C src/include install",
]

[dependencies]
build = ["pkgconf"]
runtime = ["openssl"]
